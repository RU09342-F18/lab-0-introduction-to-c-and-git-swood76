#include <stdio.h>
#include <stdlib.h>
#include "math.h"
int main()
{
    int num1;
    int num2;
    char Operator;
    //implementation (function call) of math function which is defined in main.c file
    math(num1, num2, Operator);
    return 0;
}
